#include "main.h"
#include "dht11.h"
#include "delay.h"

extern TIM_HandleTypeDef htim2;

int main(void)
{
    HAL_Init();
    SystemClock_Config();
    MX_GPIO_Init();
    MX_TIM2_Init();

    HAL_TIM_Base_Start(&htim2);

    DHT11_Data dht;

    while (1)
    {
        if (DHT11_Read(&dht) == 0)
        {
            // dht.temperature  (°C)
            // dht.humidity     (%)
        }

        HAL_Delay(2000); // Minimum DHT11 sampling period
    }
}
